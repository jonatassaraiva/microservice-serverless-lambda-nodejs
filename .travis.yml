language: node_js
node_js:
  - "6"

cache:
  directories:
    - node_modules

before_script:
  - npm install

script:
  - npm run lint
#  - npm run test

after_success:
  - chmod +x ./scripts/*.sh

deploy:
  provider: script
  script: 'node_modules/.bin/serverless deploy'
  on:
    all_branches: true
#  - npm run codacy
# - npm run deploy